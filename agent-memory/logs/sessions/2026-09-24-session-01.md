# Session 2026-09-24-01

## Session summary
A cloud session with **no database and no data files** (Docker is not
available; data/ is empty). Ben pasted the holdout report and asked what it
means. Runs 1–4 were explanations only (no files changed). In run 5 Ben asked
two things: research the Vietnamese trading fee, and add and analyse the
missing numbers.
- **Fees:** researched from web search results (the broker pages were blocked
  by the network). The realistic all-in round trip is 0.16–0.60%, and 0.10% is
  unreachable.
- **Missing numbers:** built `backtest/risk.py` and `describe_holdout`, with
  tests and a strict mutation check (15/16 caught; the survivor is
  equivalent). The real numbers are **NOT computed**: this needs Ben's machine
  (`uv run python -m vnstock_research.backtest.protocol describe-holdout`).
- The working branch `claude/great-bell-6wr1jq` was fast-forwarded onto
  `features/analog-backtest` (2ed5bef) before the work.
- pytest 532 passed, 24 skipped (no DB); ruff check clean.

## Run 1: explain the holdout report
- Request: "explain this" (the pasted holdout report).
- Done: read CURRENT_STATE (main's copy was stale; the holdout lives on
  `features/analog-backtest`), protocol.yaml, validation.md,
  forward_returns/evidence definitions. Explained every field, the verdicts,
  the families, regime/year splits and the fee sensitivity.
- Files: none.
- Next: none asked.

## Run 2: explain with an example
- Request: Ben did not follow the numbers; he asked for an example.
- Done: walked one invented trade (ABC, 100M VND) through signal → entry at
  t+1 open → exit → 0.40% cost → hit, then aggregated to hit/base/edge/net
  exp/de-clustering/p/regime/year.
- Files: none.

## Run 3: are these numbers convincing for choosing a stock?
- Done: the method is sound, but the result is not enough to trade on. The
  reasons: a small per-trade gain against large noise; avg win/loss and
  drawdown not reported (§8.3); these numbers describe a pattern, not a
  stock; G9 (one pick) is undefined; fee-sensitive; only 2 of 6 accepts are
  significant; a weak 2026. Proposed: confirm the fee → add the metrics →
  paper-trade with a pass bar fixed in advance.
- Found: `evidence.Level` computes avg_win/avg_loss, but the holdout report
  does not show them. Max drawdown is computed nowhere.
- Files: none.

## Run 4: Ben: "no broker fee, only a fee when buying and selling"
- Done: explained that this per-trade fee IS the modelled fee per side, and
  how to read the % off an order. Explained avg win/loss, max drawdown, losing
  streak and paper trading with worked VND examples.
- Files: none.

## Run 5: research the fee; add the missing numbers and analyse
- Request: Ben does not know his fee; research it. Yes, add the missing
  numbers and analyse.
- Done:
  - Fee research → context-vietnam.md. Tax 0.10% on sale; exchange fee
    0.03% a side passed on even by zero-fee brokers (DNSE, TCBS, Pinetree);
    commissions 0–0.35% a side (cap 0.5%). All-in: 0.16% / 0.40% / 0.60%.
  - Sensitivity, read exactly off the report's break-even column (NET is
    linear in the gross): at 0.16% → 10 ACCEPT; at 0.40% → 6; at 0.60% → 5
    (higher_lows drops out).
  - Built `backtest/risk.py` + `protocol.occurrences`, `describe_holdout`,
    `describe_report`, `run_describe_holdout` and the CLI `describe-holdout`.
    Split `_holdout_gated` out of `run_holdout`.
  - Tests: 11 in test_risk.py, 9 in test_holdout.py.
  - Mutation check (script in the scratchpad, not in the repo): 16
    mutations, 15 killed. The survivor: removing `losing_windows`' short-list
    guard is equivalent (the empty mean is NaN anyway; the guard only avoids
    a numpy warning). A first pass had 4 survivors; the tests were
    strengthened for 3 of them (date order with interleaved days, a
    zero-total window, de-clustering with a signal every 2nd session).
- Files created: `src/vnstock_research/backtest/risk.py`,
  `tests/test_risk.py`, this file.
- Files modified: `src/vnstock_research/backtest/protocol.py`,
  `tests/test_holdout.py`, `agent-memory/CURRENT_STATE.md`,
  `agent-memory/logs/INDEX.md`, `knowledge/code-map.md`, `decisions.md`,
  `open-questions.md`, `context-vietnam.md`.
- Decisions: see decisions.md, "Session 2026-09-24-01".
- Correction logged: open-questions and the holdout report call 0.10% all-in
  "a zero-fee broker". Real zero-fee brokers still pass on 0.03% a side, so
  the realistic floor is 0.16%. The note is fixed; the committed holdout
  report text is left as it is (it is a record of the run).
- Problems / unfinished:
  - **The describe numbers are NOT computed** (no DB here).
  - The fee is still unknown; `costs.yaml` is unchanged (still 0.15%/side,
    provisional).
  - The fee research rests on search-result summaries. Its sources partly
    disagree about SSI (0.05–0.1% in one, 0.15–0.25% in another).
- Next step: Ben runs `describe-holdout` on his machine and pastes the
  report; then analyse it, and decide the broker/fee.
