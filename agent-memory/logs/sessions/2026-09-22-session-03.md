# Session 2026-09-22-03

## Session summary
Features phase (doc §4–5) opening session. Corrected two stale lines in
CURRENT_STATE.md, and while checking one of them found that restatement
detection (G2) is not merely unfinished — the nightly job hard-codes
`restated = False` and prints "restatement detected: False" as though it had
checked. Recorded the two review findings Ben supplied about the limit
definitions and the missing return generator. Proposed the features module
layout and stopped for approval before writing any measure.

## Run 1: stale-note fix, review findings, features layout proposal

**Request**
Fix the stale G3 line in CURRENT_STATE.md and word G2 to match reality. Record
two review findings in open-questions.md without acting on them. Then propose
the features module layout and wait for approval before building the §4.1
volume measures.

**Done**
- Read CURRENT_STATE.md, knowledge/00-index.md, then money-flow.md, patterns.md
  (parameters section), code-map.md. Did not scan the repo.
- **Correction logged:** CURRENT_STATE.md listed G3 as "proposal awaiting Ben"
  while the same file had a "G3 — closed and implemented" section four
  paragraphs later. G3 moved to the closed list.
- **G2 checked against the code, not assumed.** `scripts/nightly_update.py:248`
  reads `restated = False  # today's own rows are new, so nothing to compare
  yet`, and line 250 prints "restatement detected: {restated}". The job never
  re-derives past factors and never compares them with the stored series, so
  G2 is fully open — and the log line is misleading, which is worse than
  silence. Both recorded.
- Added Ben's two review findings to open-questions.md under a new "For the
  backtest step" heading, marked do-not-act-now.
- Proposed the features module layout; awaiting approval.

**Files created / modified / deleted**
- Created: this file.
- Modified: `agent-memory/CURRENT_STATE.md`,
  `agent-memory/knowledge/open-questions.md`, `agent-memory/logs/INDEX.md`.
- No source files touched.

**Decisions**
- None made; the layout is a proposal and CLAUDE.md requires asking before an
  architectural decision.

**Problems, surprises, unfinished items**
- The misleading "restatement detected" line is a small thing that would have
  cost a lot later: a nightly log that reports a check it never ran is exactly
  how stale adjusted prices reach research unnoticed. Fix belongs with the G2
  work, not here.
- No features code written this run — approval gate.

**Next step**
Ben approves or amends the layout. Then build the §4.1 volume measures on
matched volume only, with tests that fail without each measure.
