# Session 2026-09-22-01

## Session summary
First session on the repo. The repo contained only the knowledge document.
Renamed it to `docs/knowledge/pattern-research-knowledge.md`, wrote `CLAUDE.md`
from Ben's exact text, and completed Phase 1: read the whole document and built
the `agent-memory/` structure with all knowledge files, the decisions table, the
open-questions list and this log.

Ben confirmed Phase 1, it was committed (86bd75d), the 11 gaps found in the
document were promoted to a marked "must resolve before any measurement"
section, and Phase 2 built the project skeleton: uv/ruff/pytest/pre-commit,
five empty documented modules under `src/vnstock_research/`, provisional
pattern thresholds in `config/rules/patterns.yaml`, a TimescaleDB
docker-compose, `.env.example`, `.gitignore` and a README written for someone
new to Docker. Neither `uv` nor Docker is installed on Ben's machine, so those
config files are written but unverified. Run 3 then re-checked the environment: uv was in
fact installed, so the whole skeleton was verified for real (tests, ruff,
pre-commit, compose config), the interpreter was pinned to 3.12, and the last environment gap
was Docker. Run 4 closed it: Ben launched Docker Desktop and started the
database, which is up, healthy, and already carries the TimescaleDB extension.
Phase 2 is now fully verified. Phase 3 (the SSI probe) is next and needs Ben's
confirmation plus SSI credentials.

## Run 1: Phase 1 — set up CLAUDE.md and build the memory system

**Request**
Move the knowledge doc to `docs/knowledge/pattern-research-knowledge.md`, write
`CLAUDE.md` word for word from Ben's text, then do Phase 1: read the document
completely, build `agent-memory/` with every knowledge file written in my own
words, record the decisions already made in the doc, create session 01 and
`CURRENT_STATE.md`, then stop and report with a summary, the five hardest
things, and any contradictions or gaps found.

**Done**
- Renamed the knowledge document (git mv) to the required path. Not edited.
- Wrote `CLAUDE.md` verbatim (106 lines).
- Read all 507 lines / 11 sections of the document.
- Created `agent-memory/` with `CURRENT_STATE.md`, `knowledge/` (13 files),
  `logs/INDEX.md` and this session file.
- Knowledge files are my own understanding with doc-section references, not
  copies of the text.
- Recorded 13 decisions in `knowledge/decisions.md`, including the five Ben
  named (stack, 2012 window, rules-based, no message broker, EOD first).
- Logged 22 open questions, split into "for Ben", "for the broker friend" and
  "technical/factual", including six gaps I found in the document itself.

**Files created / modified / deleted**
- Renamed: `docs/knowledge/VN Stock Pattern Researcher — Knowledge Document.md`
  → `docs/knowledge/pattern-research-knowledge.md`
- Created: `CLAUDE.md`
- Created: `agent-memory/CURRENT_STATE.md`
- Created: `agent-memory/knowledge/00-index.md`, `project.md`, `patterns.md`,
  `money-flow.md`, `context-vietnam.md`, `research-findings.md`,
  `funnel-and-scale.md`, `validation.md`, `architecture.md`,
  `data-sources.md`, `code-map.md`, `decisions.md`, `open-questions.md`
- Created: `agent-memory/logs/INDEX.md`,
  `agent-memory/logs/sessions/2026-09-22-session-01.md`
- Deleted: nothing.

**Decisions** (copied to `knowledge/decisions.md`)
- All decisions this run are ones already made in the document; I recorded them
  rather than making them. The one new entry is procedural: Phase 3 probes SSI
  before any schema design, because docs and real payloads often differ.

**Problems, surprises, unfinished items**
- The document is internally consistent on strategy but has concrete gaps that
  will bite during implementation: volume is never adjusted alongside prices
  for stock dividends/rights; SSI's adjusted close may be re-stated after each
  new corporate action, which would make stored history drift on every nightly
  update; ticker changes, mergers and HNX↔HOSE transfers are not mentioned;
  the §2 forward-return definition (3/5 days from the signal day) is not the
  tradeable return once entry is t+1 and T+2 applies; §8's anti-overfitting
  defences cover single-pattern testing but not the analog/fingerprint
  similarity search of §3.6, which has its own free parameters; and DailyStock-
  Price's 10 pages × 1,000 rows cap means a whole-market range request covers
  only about six trading days, which shapes the download plan.
- Two things are stated both ways: market regime as a *filter* (§7.2 step 3)
  and as a *reported dimension* (§5.3); and "full history is a must, the friend
  studied from 2010" (§7.5) against the 2012 start.
- "Rank by evidence strength" (§7.2 step 7) — the function that picks one stock
  from the final handful — is never defined.
- Nothing is verified against a live API; `data-sources.md` records everything
  as unconfirmed on purpose.

**Next step**
Wait for Ben's confirmation, then Phase 2 (skeleton: uv/ruff/pytest, empty
modules, docker-compose with TimescaleDB, .env.example, README).

## Run 2: Phase 2 — project skeleton

**Request**
Commit Phase 1. Add every gap found to `open-questions.md`, marked "must resolve
before any measurement". Then proceed to Phase 2: the project skeleton.

**Done**
- Promoted the 11 gaps found in run 1 into a dedicated, clearly marked section
  of `open-questions.md` (G1–G11), each with what it breaks and how to resolve
  it. Removed the duplicated bullets from the old technical section.
- Committed Phase 1 as 86bd75d (initial commit).
- Built the Phase 2 skeleton exactly to Ben's layout: `pyproject.toml`
  (Python ≥3.12, uv, ruff, pytest, pre-commit; runtime deps deliberately
  empty), `.pre-commit-config.yaml`, `.gitignore`, `.env.example`,
  `docker-compose.yml` (PostgreSQL + TimescaleDB only, no Redis),
  `src/vnstock_research/{data,features,patterns,backtest,report}` as empty
  modules each with a docstring naming its doc section, `config/rules/
  patterns.yaml`, git-ignored `data/raw/` and `data/processed/`, `scripts/`,
  `tests/`, `notebooks/`, and a `README.md` that explains what Docker,
  images, containers and volumes are before asking anyone to run a command.
- Wrote `tests/test_skeleton.py` — four checks: the package imports, every
  stage has a docstring citing a doc section, thresholds live in config, and
  `.gitignore` covers `.env` and `data/`.

**Files created / modified / deleted**
- Modified: `agent-memory/knowledge/open-questions.md`, `code-map.md`,
  `decisions.md`, `agent-memory/CURRENT_STATE.md`, this file.
- Created: `README.md`, `pyproject.toml`, `.pre-commit-config.yaml`,
  `.gitignore`, `.env.example`, `docker-compose.yml`,
  `config/rules/patterns.yaml`, `tests/test_skeleton.py`,
  `src/vnstock_research/__init__.py` and `{data,features,patterns,backtest,
  report}/__init__.py`, `data/raw/.gitignore`, `data/processed/.gitignore`,
  `scripts/.gitkeep`, `notebooks/.gitkeep`.
- Deleted: nothing.

**Decisions** (copied to `knowledge/decisions.md`)
Eight tooling decisions: ruff as linter *and* formatter; empty runtime
dependencies until a phase needs one; hatchling + `src/` layout; pytest
`pythonpath = ["src"]` instead of an editable install; the
`timescale/timescaledb:latest-pg16` image; a named Docker volume rather than a
bind mount; local DB credentials as defaults in `.env`; and seeding
`config/rules/patterns.yaml` with provisional textbook values rather than
leaving the folder empty. None of these change the architecture the document
sets out, but they were made without asking — flagged to Ben in the reply.

**Problems, surprises, unfinished items**
- **`uv` and Docker are both missing from Ben's machine** (`uv: not found`,
  `docker: not found`; system Python is 3.9.6). So `uv sync`,
  `uv run pytest`, `uv run ruff check` and `docker compose up -d` have never
  been executed here. `pyproject.toml`, `.pre-commit-config.yaml` and
  `docker-compose.yml` are written but **unverified**.
- PyYAML is not in the system Python either, so the YAML files could not even
  be parse-checked. They were written carefully but nothing machine-checked
  them.
- `tests/test_skeleton.py` was run by hand on system Python 3.9 (importing the
  test module directly, no pytest). All four checks pass. The docstring check
  caught a real inconsistency on the first run — `data/__init__.py` wrote
  "Doc §7.5" with a capital D — so the assertion was made case-insensitive.
- Nothing from Phase 2 is committed yet; Ben asked for the Phase 1 commit only.

**Next step**
Ben installs uv and Docker and runs `uv sync` + `docker compose up -d` once to
verify. Then Phase 3: `scripts/probe_ssi_history.py`, which needs real SSI
FastConnect credentials in `.env`.

## Run 3: verify uv and Docker

**Request**
Before confirming Phase 3, check uv and Docker.

**Done**
- Searched properly instead of trusting a bare `which`. **uv is installed** at
  `~/.local/bin/uv` (0.12.17) — run 2's "not found" was wrong, and the honesty
  rule applies: the machine is right, the note was wrong. Corrected everywhere.
- Ran the whole skeleton for real: `uv sync` (17 packages), `uv run pytest`
  → 4 passed, `uv run ruff check .` → clean, `uv run ruff format --check .`
  → 26 files already formatted, `uv run pre-commit run --all-files` → 7 hooks
  passed, and PyYAML parsed all three YAML files.
- **Found a real defect:** `requires-python = ">=3.12"` let uv install Python
  **3.13.15**, not the 3.12 Ben specified. Added `.python-version` with `3.12`
  and re-synced — now Python 3.12.14, all checks still pass.
- Docker: `/Applications/Docker.app` exists but has never been launched, so the
  `docker` command is not on PATH and no daemon is running. Validated
  `docker-compose.yml` anyway by calling the bundled compose plugin directly
  (`docker compose config`) — the file is valid and every default expands
  correctly. Did **not** launch Docker Desktop: its first run wants an admin
  password and a licence acceptance, which is Ben's to give.
- Updated the README: the Docker section now explains that the first launch is
  what puts `docker` on PATH (the exact state this machine was in), and the
  setup section explains the 3.12 pin.

**Files created / modified / deleted**
- Created: `.python-version`, `uv.lock` (both to be committed), `.venv/`
  (git-ignored).
- Modified: `README.md`, `agent-memory/knowledge/open-questions.md`,
  `code-map.md`, `decisions.md`, `agent-memory/CURRENT_STATE.md`, this file.

**Decisions** (copied to `knowledge/decisions.md`)
- Pin the interpreter with `.python-version` rather than capping
  `requires-python` at `<3.13`: the project gets 3.12 without forbidding a
  future move.

**Problems, surprises, unfinished items**
- Run 2 reported uv as missing. It was not. The lesson recorded here: check the
  install locations, not just `which` in whatever shell happened to start.
- The Python version drift would have gone unnoticed if the environment had
  never been run — a reminder that "written but unverified" is not "done".
- The database container has still never started. `docker compose up -d` is
  unproven until Ben opens Docker Desktop once.

**Next step**
Ben opens Docker Desktop once and runs `docker compose up -d`. Then Phase 3:
`scripts/probe_ssi_history.py`, which needs SSI credentials in `.env`.

## Run 4: verify the database container

**Request**
Check again — Ben has run `docker compose up -d`.

**Done**
- Confirmed Docker Desktop is running and the daemon is reachable (server
  29.8.0).
- `vnstock-db` is **up and healthy**, 31 seconds old, publishing 5432, from
  `timescale/timescaledb:latest-pg16`. The healthcheck in `docker-compose.yml`
  works — the container reports `(healthy)`, not merely `Up`.
- Ran the exact psql command the README gives a newcomer:
  `docker compose exec db psql -U vnstock -d vnstock` → **PostgreSQL 16.15**.
  It works as written, so the README's credentials and service name are right.
- Checked `pg_extension`: **timescaledb 2.30.1 is already created** in the
  `vnstock` database. Useful and previously unknown — the schema phase will not
  need a `CREATE EXTENSION timescaledb` step.
- Named volume `stock_assistant_vnstock-db-data` exists, so data will survive
  `docker compose down`.

**Files created / modified / deleted**
- Modified: `agent-memory/CURRENT_STATE.md`,
  `agent-memory/knowledge/open-questions.md`, `code-map.md`, this file,
  `logs/INDEX.md`. No source changes — nothing needed fixing.

**Decisions**
- None.

**Problems, surprises, unfinished items**
- The `docker` command is still not on *this session's* PATH, because Docker
  Desktop installs its CLI into `~/.docker/bin` (added by `~/.zprofile`) and
  this session's environment was captured before Docker's first launch. Not a
  problem for Ben — any new terminal has it. Recorded in `open-questions.md`
  so a future session does not repeat the confusion: the binaries are at
  `/Applications/Docker.app/Contents/Resources/bin/docker` and
  `.../cli-plugins/docker-compose`.
- Phase 2 is still not committed; Ben has not asked for it yet.

**Next step**
Commit Phase 2 on Ben's word, then Phase 3: `scripts/probe_ssi_history.py`,
which needs SSI FastConnect credentials in `.env`.
