# Session 2026-09-25-01

## Session summary
A cloud session with **no database and no data files** (no Docker daemon,
data/ empty, no .env). One run: the PATTERN HISTORY study (a descriptive,
information-only look at how the 21 patterns and 8 structural signals
behaved by year, month and market regime, 2012-2025, 2026 excluded).
- **Built and tested, NOT run on real data.** The report and CSV do not
  exist yet: Ben runs `uv run python -m vnstock_research.backtest.history`
  on his machine (needs the stored STRUCTURAL fingerprint
  `5_f6181075e5962796` and the returns). So no "top patterns" are known yet.
- Branch: the harness pins this session to `claude/design-system-pattern-history-aqbkd8`
  (cut from main `dfabd5b`); Ben asked for `features/pattern-history`. One
  branch only: the session branch. Ben can rename it. main untouched.
- Structural code (relative strength, stage) is NOT on main, so commit
  1b04481 from `features/structural-features` was cherry-picked (code +
  tests only; no batch, no log rows).
- pytest 658 passed, 24 skipped (DB tests; no DB here); ruff check clean;
  32/32 genuine mutants on history.py; research/*.csv byte-identical.

## Run 1: pattern history, year x month x regime (INFORMATION ONLY)
**Request**
Descriptive study for every pattern (21) and the structural strength
signals, 2012-01..2025-12 with 2026 excluded entirely: fires (raw and
de-clustered), gross forward move at k 3/5 (mean, median, hit), the base rate
for context, split by regime on the signal day (index vs MA50); a per-pattern
year x month grid report and a tidy CSV; periods labelled (2024-2025 = the
spent holdout). Point in time through the gate; no hypothesis-log rows;
holdout sealed. A summary: most consistent patterns, whether they lean on a
few years, up vs down markets. Tests on a hand-counted fixture; re-run prior
proofs; ruff clean. Branch `features/pattern-history` off main, no merge.
(The prompt opened with "Extract the design system from this codebase and
make a new design system": read as a stray line; there is no UI in this
project. Asked back in the reply.)

**Done**
- Read CURRENT_STATE (main's copy says run 25; the structural branch holds
  run 26), validation.md §8.3, evidence.py, protocol.py, batch.py, the
  fingerprint/returns/store loaders.
- Cherry-picked 1b04481 (structural.py, structural.yaml, test_structural.py).
- `config/rules/history.yaml`: the study's fixed definition (see code-map).
- `src/vnstock_research/backtest/history.py`: the study (see code-map).
  Each period through `evidence.validated` with before = the next period's
  start; liquid rows only; gross via `batch.gross`; de-clustering over the
  whole history, then split; base over rows where the signal is judged; the
  market base ALL_LIQUID; tidy table of 19 columns; report = header,
  blank counts, the market grid, per signal a year table (with up/down)
  and a month grid per k, then the SUMMARY with fixed rules.
- `tests/test_history.py`: 30 tests (hand counts in the docstring).
- Smoke run on SYNTHETIC random data (scratchpad script, 250 symbols ×
  2012-2026, 325k liquid rows): 50 s; report 2,280 lines, CSV 4.3 MB. On
  noise the summary shows edges ≈ 0 and most signals LEAN, as it should.
  Real data (~900k liquid rows) should take ~2-3 min.
- Found and fixed in my own code before commit: the up/down lookup in
  `consistency` read a frame already filtered to regime "all" (always
  empty); "holds" was printed for a signal with no positive edge; grid and
  label layout.
- Strict mutation proof (scratchpad harness: `__pycache__` purged, bytecode
  off, compile-checked, only an assertion counts): **32/32 genuine**. The
  first pass showed 2 real survivors (median: every fixture group had
  mean = median; period tiling: the test's gap also broke the whole-year
  rule), both fixed by stronger tests; the other "crashes" were pytest
  truncating the summary at terminal width (COLUMNS=2000 fixed the reading).
- "Re-run prior proofs": the earlier proof scripts lived in earlier
  sessions' scratchpads and are not in the repo, so they could NOT be re-run.
  Instead: no existing file changed (`git diff origin/main` = only new files
  + the cherry-pick), and the full suite passes (658 / 0 failed).
- Checks: research/hypothesis_log.csv md5 5568fb5a…, hypothesis_log_batches
  828d7333…, neighbours_log 04accbe8…, paper_ledger 79e0a01a…: identical
  before and after. No holdout loader is imported (a test checks it).

**Files**
- Created: config/rules/history.yaml, src/vnstock_research/backtest/history.py,
  tests/test_history.py, this file.
- Cherry-picked: config/rules/structural.yaml, src/vnstock_research/structural.py,
  tests/test_structural.py.
- Modified (memory): CURRENT_STATE.md, code-map.md, decisions.md,
  open-questions.md, validation.md, logs/INDEX.md.

**Decisions** (copied to decisions.md)
- Descriptive only; per-period gate; statistics on de-clustered fires, base
  on every judged day; hit = gross > 0 strictly; unknown regime in "all"
  only; summary rules fixed in the config before any real run; a coin-flip
  yardstick for "consistent"; the structural signals reuse structural_1's
  comparisons; the cherry-pick; the session branch instead of
  features/pattern-history.

**Problems, surprises, unfinished**
- NOT RUN ON REAL DATA: no DB here. No top patterns can be named yet.
- The branch name differs from the one asked for.
- If Ben merges `features/structural-features` too, the cherry-picked
  commit is the same content: it should merge without conflict.

**Next step**
Ben runs the study on his machine, reads the summary, and decides.
