# Session 2026-09-23-02

## Session summary
Ben interrupted this run midway and continues in a new session. Branch
`features/pit-universe-breadth` was cut from main at `4f36815`, after PR #1
merged. The database was unreachable here: no Docker daemon, nothing on :5432,
no `.env`, and the CafeF host is blocked by this container's network policy
(proxy 403). So Task 1 (the 5 sessions missing from the index) is **not
resolved**. I built a read-only diagnostic for it instead. Task 2 (the PIT
universe + breadth design proposal) is **not started**.

## Run 1: orient; index-gap follow-up; PIT universe + breadth proposal (INTERRUPTED)
**Request**
Step 0: orient and report. Task 1: print the 5 trading_day sessions since 2012
with no VNINDEX row, and find out whether each is a phantom calendar session or
an index ingest gap; fix only if the cause is unambiguous. Task 2: propose the
PIT universe + breadth design, then STOP for approval (no code).

**Done**
- Oriented from memory: CLAUDE.md, CURRENT_STATE, 00-index, code-map, and the
  features parts of decisions/open-questions. Read the code this task touches:
  `features/{base,bars,market}.py`, the trading_day build in
  `scripts/load_history.py` and `scripts/nightly_update.py`, and the calendar
  checks in `data/checks.py`.
- Doc check: breadth is in doc **§5.3** (line 202). §5.4 is sector. Ben's
  prompt said §5.4.
- Finding from the code (no DB needed): `trading_day` is "any
  (date, exchange) with ≥1 row in bar_raw". Its only calendar check rejects
  WEEKENDS. Nothing rejects a weekday HOLIDAY with a stray row. There is a
  second possible cause: weekend INDEX rows are rejected at parse rather than
  shifted, so a CafeF index row dated a day late could drop a real Friday.
  These are hypotheses until the dates are printed.
- Created `scripts/investigate_index_gaps.py` (read-only). For each missing
  session it prints the per-exchange symbol count vs the neighbouring median,
  date-shifted / backfilled / zero-volume row counts, other index rows that
  day, the adjacent VNINDEX dates, and the stray rows when there are ≤15. It
  also prints a hint: PHANTOM / INDEX-GAP / UNCLEAR.
- Created `tests/test_index_gap_diagnostic.py`, 5 tests of the hint logic.
- Smoke-tested the SQL against a throwaway local Postgres 16 built from
  migrations 001/003/004/005 and seeded with one fake case of each kind. The
  fake holiday with 2 stray rows printed PHANTOM-LIKELY. The fake full session
  with no index row printed INDEX-GAP-LIKELY. That was synthetic data, NOT the
  real DB.
- `uv run pytest` → 74 passed, 4 skipped (the DB tests). `uv run ruff check .`
  → clean.

**Files created / modified / deleted**
- Created: `scripts/investigate_index_gaps.py`,
  `tests/test_index_gap_diagnostic.py`, this file.
- Modified: `agent-memory/logs/INDEX.md`.

**Not done yet**
- Task 1 on real data: run `uv run python scripts/investigate_index_gaps.py`
  with the DB up, record the 5 dates and the cause, and fix only if the cause
  is unambiguous.
- Task 2: the PIT universe + breadth proposal. Nothing written yet.
- End-of-run memory: CURRENT_STATE.md NOT rewritten, and code-map.md not yet
  updated for the two new files. The stale header paragraphs in code-map.md
  ("nightly job NOT built", "no functions yet") are still to fix.
- Nit spotted: `scripts/report_features.py` docstring says to run
  `report_volume_features.py`, a name that no longer exists.

**Next step**
The new session reads this file, then runs the diagnostic with the DB up, and
then writes the Task 2 proposal.
